---
课程: 华为云 HCCDA-Tech Essentials 云技术精髓入门级开发者认证
章节: 4. 云网络互联
周次: 第一周
日期: 2026-09-11
标签: [华为云, ICT大赛, 云赛道]
---

# 4 云网络互联

> 一句话概括：云上一切资源都要“联网”——VPC 提供隔离的虚拟网络底座，安全组 / 网络 ACL 做分层防护，ELB 分发流量，VPN / NAT 网关 / EIP 打通云上云下与公网。

## 学习目标

- 掌握 **VPC** 的定义、相关概念（子网、EIP、安全组、对等连接、网络 ACL 等）、产品架构、优势、配置流程与应用场景。
- 分清淡入理解 **安全组** 与 **网络 ACL** 的区别（作用对象、层级、规则状态）。
- 掌握 **ELB** 的三大组成（负载均衡器 / 监听器 / 后端服务器）与常见应用场景。
- 掌握 **VPN** 的两大组成（VPN 网关 / VPN 连接）与组网方式。
- 掌握 **NAT 网关** 的 SNAT / DNAT 功能与典型场景。
- 了解 **云解析服务 DNS** 等其他网络服务。

## 正文

### 4.1 虚拟私有云 VPC

#### 4.1.1 定义

**虚拟私有云（Virtual Private Cloud，VPC）** 是云服务器、云容器、云数据库等资源构建的**隔离的、用户自主配置和管理**的虚拟网络环境，用于提升云上资源安全性、简化网络部署。

#### 4.1.2 相关概念

| 概念 | 说明 |
| --- | --- |
| 子网 | VPC 内的 IP 地址块。所有云资源必须部署在子网内；同一 VPC 下子网网段不可重复，创建后**网段不可修改** |
| 弹性公网 IP（EIP） | 独立的公网 IP 资源（含公网 IP 与公网出口带宽），可与 ECS、BMS、VIP、ELB、NAT 网关等灵活绑定 / 解绑；**一个 EIP 只能绑定一个云资源** |
| 安全组 | 逻辑分组，为具有相同安全保护需求并相互信任的云服务器提供访问策略；系统默认创建 **Sys-default** 安全组（出方向全放行、入方向受限，组内互访无需加规则） |
| 对等连接 | 两个 VPC 之间的网络连接，用私网 IP 通信；**同一区域内**才能创建（含跨账号 VPC），**不同区域间不能创建** |
| 网络 ACL | **子网级别**的可选安全层，通过出 / 入方向规则控制出入子网的数据流，用于增加额外安全防护层 |
| 虚拟 IP（VIP） | 未分配给真实 ECS 网卡的 IP，ECS 可同时拥有私有 IP 与 VIP，可通过任一 IP 访问 |
| 弹性网卡 | 虚拟网卡，可创建并附加到 ECS / BMS 实例上，实现灵活、高可用的网络方案 |
| 二层连接网关（L2CG） | 虚拟隧道网关，基于云专线建立云上云下**二层**网络互通，可在不改变子网 / IP 规划前提下迁移业务上云 |
| IP 地址组 | 多个 IP 的集合，可被安全组规则引用，便于批量管理需频繁修改的 IP |

> 关键规则：**默认同一 VPC 内所有子网可互通，不同 VPC 间默认不能通信**（需通过创建**对等连接**互通）。

#### 4.1.3 产品架构

每个 VPC 由 **私网网段 + 路由表 + 至少一个子网** 组成：

- **私网网段**：创建 VPC 时指定，支持 `10.0.0.0/8~24`、`172.16.0.0/12~24`、`192.168.0.0/16~24`。
- **子网**：云资源必须部署在子网内，子网网段必须在私网网段内。
- **路由表**：创建 VPC 时系统自动生成**默认路由表**，保证同 VPC 下所有子网互通；默认路由无法满足需求时（如未绑 EIP 的 ECS 访问外网）可创建自定义路由表。
- **安全**：安全组（防护 ECS）+ 网络 ACL（防护子网）。
- **VPC 连接**：
  - 同一区域不同 VPC 私网互通 → **VPC 对等连接**；
  - VPC 与公网互通 → **EIP 或 NAT 网关**；
  - VPC 与数据中心连通 → **VPN、云连接、云专线、VPC 二层连接网关 L2CG**。

#### 4.1.4 优势

| 优势 | 说明 |
| --- | --- |
| 安全可靠 | VPC 间通过隧道技术 **100% 逻辑隔离**；安全组防护 ECS，网络 ACL 防护子网 |
| 灵活配置 | 自定义私网、按需划分子网、配置 IP / DHCP / 路由表；支持跨可用区部署 ECS |
| 高速访问 | 全动态 **BGP** 接入多运营商，支持多达 **21 条线路**，实时自动故障切换，时延低 |
| 互联互通 | 通过 EIP、ELB、NAT 网关、VPN、云专线连公网；通过对等连接实现 VPC 私网互通 |

#### 4.1.5 配置流程

创建 VPC → 配置子网（默认子网不满足时新建；子网默认启用 **DHCP**，ECS 启动后自动获取 IP）→ 配置安全组（默认安全组 + 自定义出 / 入方向规则）。

- **入方向**：从外部访问安全组规则下的 ECS。
- **出方向**：安全组规则下的 ECS 访问安全组外的实例。

#### 4.1.6 应用场景

- **云端专属网络**：每个 VPC 是一个私有网络，与其他 VPC 逻辑隔离；生产 / 测试环境用多个 VPC 隔离，需互通时建**对等连接**。
- **Web 应用或网站托管**：通过 EIP 或 NAT 网关连接 ECS 与 Internet，结合 **ELB** 将流量均衡分配到多台 ECS。
- **Web 应用访问控制**：将多层 Web 应用划分到不同安全组（如 Web 服务器子网可访问互联网、数据库服务器仅内网访问），满足高安全场景。

### 4.1.7 安全组 vs 网络 ACL（重点对比）

| 对比项 | 安全组 | 网络 ACL |
| --- | --- | --- |
| 作用对象 | 弹性云服务器 / 云资源（实例级） | 子网（子网级） |
| 是否必选 | 必选（默认有 Sys-default） | 可选 |
| 是否有状态 | 有状态（放行请求后自动放行返回流量） | 无状态（出 / 入方向均需单独配置规则） |
| 规则方向 | 出方向 / 入方向 | 出方向 / 入方向 |
| 典型用途 | 定义实例的访问策略 | 增加额外的子网级安全防护层 |

### 4.2 弹性负载均衡 ELB

#### 4.2.1 定义

**弹性负载均衡（Elastic Load Balance，ELB）** 是将访问流量根据分配策略分发到后端多台服务器的**流量分发控制服务**，可扩展应用对外服务能力，并通过消除单点故障提升可用性。

#### 4.2.2 产品架构（三部分）

| 组成 | 作用 |
| --- | --- |
| **负载均衡器** | 接受客户端传入流量，并将请求转发到一个或多个可用区的后端服务器 |
| **监听器** | 一个 LB 可添加多个监听器；按配置的协议 / 端口检查客户端请求，并按分配策略转发到后端服务器组 |
| **后端服务器** | 每个监听器绑定一个后端服务器组，组内可添加多台后端服务器；可为**后端服务器**配置转发权重（**不能为服务器组配置权重**）；支持**健康检查**，异常时自动剔除、恢复后自动重新纳入 |

#### 4.2.3 优势

- **高性能**：集群支持最高 **1 亿并发连接**。
- **高可用**：集群化部署，支持多可用区**同城双活容灾**，无缝实时切换。
- **灵活扩展**：按流量自动分发，与**弹性伸缩 AS 无缝集成**。
- **简单易用**：快速部署、实时生效，支持多种协议与多种调度算法。

#### 4.2.4 配置流程

创建负载均衡器（规划区域 / 类型 / 协议 / 后端服务器）→ 配置监听器（流量分配策略）→ 添加 ECS 作为后端服务器。

#### 4.2.5 使用管理注意事项

- 删除 ELB 后无法恢复，操作需谨慎；删除**公网型** LB 时，绑定的 **EIP 不会默认自动删除**。
- 监听器删除后无法恢复；移除后端服务器只是解除关联，不影响服务器本身。

#### 4.2.6 应用场景

- **高访问量业务**：均匀分发到多台后端服务器（门户网站、应用市场），可开会话保持。
- **潮汐业务（ELB + AS）**：大促等短期流量高峰，弹性扩缩 ECS 并自动加入 / 移出后端组（如双 11、618）。
- **消除单点故障**：健康检查及时屏蔽故障服务器并转发到健康节点。
- **跨可用区容灾部署**：流量跨 AZ 分发，某 AZ 故障仍可转发（银行、警务、大型应用）。

### 4.3 虚拟专用网络 VPN

#### 4.3.1 定义

**虚拟专用网络（Virtual Private Network，VPN）** 用于在远端用户与 VPC 之间建立一条**安全加密的公网通信隧道**。默认 VPC 内 ECS 无法与用户数据中心 / 私有网络通信，需要连通时可启用 VPN。

#### 4.3.2 组网（两大组成）

| 组成 | 说明 |
| --- | --- |
| **VPN 网关** | VPC 中建立的**出口网关设备**，与本地数据中心的远端网关配合使用；一个本地数据中心绑定一个远端网关，一个 VPC 绑定一个 VPN 网关；支持点到点 / 点到多点，故网关与远端网关为**一对一或一对多**关系 |
| **VPN 连接** | 基于 Internet 的 **IPsec 加密技术**，构建 VPN 网关与远端网关之间的加密通道；当前支持 **IPsec VPN** 协议，使用 **IKE 和 IPsec** 对传输数据加密 |

#### 4.3.3 优势

- **高安全**：华为专业设备，基于 IKE 和 IPsec 加密，电信级高可靠（硬件 / 软件 / 链路三层保障）。
- **无缝扩展资源**：本地数据中心与云上 VPC 互联，实现混合云部署。
- **连通成本低**：利用 Internet 构建 IPsec 通道，费用比**云专线**更便宜。
- **即开即用**：部署快速、实时生效。

#### 4.3.4 应用场景

- **单站点 VPN 连接**：一个本地数据中心与 VPC 快速连接，构建混合云。
- **多站点 VPN 连接**：多个本地数据中心与 VPC 快速连接。

#### 4.3.5 配置流程与关键参数

创建 **VPN 网关** → 创建 **VPN 连接**。

创建 VPN 网关参数：虚拟私有云（接入的 VPC）、类型（默认 IPsec）、计费方式（**按带宽 / 按流量**）、带宽大小（该网关下所有 VPN 连接**共享**的带宽）。

创建 VPN 连接参数：

- **本端子网**：VPC 内需与数据中心互通的子网（可选择子网 / 手动输入网段）。
- **远端网关**：用户数据中心 VPN 的**公网 IP 地址**。
- **远端子网**：数据中心需与 VPC 通信的子网；**不能被本端子网网段覆盖，也不能与本端 VPC 已有的对等连接网段重合**。
- **预共享密钥（PSK）**：取值 **6~128 位**，两端配置必须一致。

#### 4.3.6 使用管理

- VPN 连接可修改 / 删除；当网关为**按需计费**时，删除**最后一个** VPN 连接会**同时删除绑定的 VPN 网关**。
- 已被 VPN 连接使用的 VPN 网关不可删除，需**先删连接再删网关**。

### 4.4 NAT 网关

#### 4.4.1 定义

**NAT 网关**提供网络地址转换服务，分为**公网 NAT 网关**和**私网 NAT 网关**：

- **公网 NAT 网关**：为 VPC 内 ECS 提供 **SNAT 和 DNAT** 功能，构建 VPC 的公网出入口。
- **私网 NAT 网关**：使多个云主机共享私网 IP 访问用户本地数据中心（IDC）或其他 VPC，也支持云主机面向私网提供服务。

#### 4.4.2 优势

- **灵活部署**：支持跨子网、跨可用区部署；规格、公网 IP 可随时调整。
- **多样易用**：多种网关规格，简单配置即用，快速发放、运行稳定。
- **降低成本**：多个云主机**共享使用 EIP**，无需为每台主机单独购买 EIP 和带宽。

#### 4.4.3 配置流程

购买公网 NAT 网关（需指定 VPC、子网、规格）→ 创建 **SNAT 规则** 和 / 或 **DNAT 规则**。

- 确认 VPC 默认路由表是否存在 `0.0.0.0/0` 默认路由；若存在需为新网关添加不同路由或在新路由表中创建 `0.0.0.0/0` 默认路由。
- **SNAT**：VPC 子网中全部 / 部分云主机共享 EIP 访问公网。**一个子网对应一条 SNAT 规则**。
- **DNAT**：通过映射（端口映射 / IP 映射）将 VPC 内云主机对互联网提供服务。**一个云主机的一个端口对应一条 DNAT 规则**。

#### 4.4.4 应用场景

- **SNAT 访问公网**：请求量大、需节省 EIP 且避免主机 IP 直接暴露时使用。
- **DNAT 面向公网提供服务**：通过端口映射 / IP 映射转发访问请求到目标主机。
- **高速访问互联网**：线下私有云 / 跨区域经云专线、VPN 接入的用户，用 SNAT / DNAT 实现安全高速上网或提供服务。
- **搭建高可用 SNAT**：一条 SNAT 规则最多可绑定 **20 个 EIP**，随机选择 EIP 访问公网；某 EIP 被攻击封堵时业务仍可用（被封堵 EIP 需手动从 EIP 池删除）。

#### 4.4.5 管理注意事项

- 公网 NAT 网关规格可调整；**提升规格不影响业务**，**降低规格**取决于当前业务量是否超过降档后规格上限。
- 删除公网 NAT 网关前，必须保证其下 **SNAT 和 DNAT 规则已全部删除**。

### 4.5 其他网络服务

- **云解析服务（DNS）**：提供高可用、高扩展的 DNS，把域名解析成 IP。**默认开通、免费使用**，提供四类解析：
  - **公网域名解析**：公网域名 ↔ IP，基于 Internet。
  - **内网域名解析**：VPC 内生效的内网域名 ↔ 私网 IP。
  - **反向解析**：由 IP 反查域名，常用于自建邮件服务器（提升邮箱 IP / 域名信誉）。
  - **智能线路解析**：按运营商 / 地域等来源返回不同解析结果，解决跨运营商 / 跨地域访问慢。
  - 域名格式要求：单段 ≤ 63 字符、总长（含末尾点）≤ 254 字符、连字符不得出现在段首 / 段尾。
- **云连接**：跨区域 / 跨 VPC 的云上网络互通（本书在 VPC 连接中提及）。
- **云专线**：为企业数据中心与云上 VPC 之间提供**专用、稳定、低时延**的连接（相比 VPN 更贵但更稳）。
- **弹性公网 IP / 共享带宽**：EIP 提供公网出口；共享带宽可让多个 EIP 共享一条带宽，降低成本。
- **二层连接网关（L2CG）**：云上云下二层互通，便于平滑迁移。

## 核心概念速查表

| 术语 | 含义 |
| --- | --- |
| VPC | 虚拟私有云，隔离的、用户自主配置的虚拟网络环境 |
| 子网 | VPC 内的 IP 地址块，云资源必须部署在子网内，网段创建后不可改 |
| 私网网段 | VPC 使用的 IP 段，支持 10/8~24、172.16/12~24、192.168/16~24 |
| 安全组 | 实例级、有状态的安全策略，默认 Sys-default |
| 网络 ACL | 子网级、无状态的可选安全层 |
| 对等连接 | 同区域两 VPC 之间用私网 IP 通信的连接 |
| EIP | 弹性公网 IP，一个 EIP 只能绑定一个云资源 |
| VIP | 虚拟 IP，未分配给真实网卡、可漂移的 IP |
| ELB | 弹性负载均衡，按策略分发流量到后端服务器 |
| 监听器 | ELB 组件，检查请求并按策略转发到后端服务器组 |
| AS | 弹性伸缩，与 ELB 集成实现潮汐业务扩缩容 |
| VPN | 虚拟专用网络，基于 IPsec 的加密公网隧道 |
| SNAT | 源地址转换，内网主机共享 EIP 访问公网 |
| DNAT | 目的地址转换，将公网请求映射到内网主机 |
| L2CG | 二层连接网关，云上云下二层互通 |
| DNS | 云解析服务，默认开通、免费，含公网 / 内网 / 反向 / 智能线路解析 |

## 考点 / 易错点

- ⚠️ **只读关键词**：VPC 私网网段支持范围、子网网段不可修改、**一个 EIP 只能绑一个资源**、默认安全组名叫 **Sys-default**。
- ⚠️ **对等连接不能跨区域**（只能同区域）；跨区域互通需用**云连接**。
- ⚠️ **安全组有状态、网络 ACL 无状态**；安全组防护实例、网络 ACL 防护子网。
- ⚠️ ELB：**可为“后端服务器”配权重，不能为“后端服务器组”配权重**；删除公网 LB **不会自动删 EIP**。
- ⚠️ VPN：**预共享密钥 6~128 位**且两端一致；按需计费网关删最后一个连接会**连带删网关**；被连接占用的网关不能删。
- ⚠️ NAT：**一个子网 = 一条 SNAT 规则**，**一个主机端口 = 一条 DNAT 规则**；删除网关前需先删所有 SNAT / DNAT 规则。
- ⚠️ 混淆点：VPN 便宜、云专线稳定；NAT 网关的核心价值是**共享 EIP、节省成本、隐藏内网**。

## 自测题

1. 判断：两个位于不同区域的 VPC 可以通过创建对等连接实现私网互通。（ ）
2. 安全组和网络 ACL 最大的区别是什么？（至少答出两点）
3. ELB 由哪三部分组成？其中哪一部分可以配置转发权重？
4. 一条 SNAT 规则最多可绑定多少个 EIP？删除 NAT 网关前必须做什么？
5. VPN 网关按需计费时，删除最后一个 VPN 连接会发生什么？

**参考答案**

1. ❌ 错。对等连接**仅支持同一区域**内的 VPC 互通，不同区域需使用云连接。
2. ①安全组作用于 **ECS 实例**、网络 ACL 作用于 **子网**；②安全组**有状态**、网络 ACL **无状态**；③安全组默认必选、网络 ACL 为可选。
3. ①**负载均衡器**；②**监听器**；③**后端服务器**。可为**后端服务器**配置转发权重（不能为后端服务器组配权重）。
4. 最多 **20 个**；删除前必须保证该 NAT 网关下的 **SNAT 和 DNAT 规则已全部删除**。
5. 若 VPN 网关计费模式为**按需**，删除最后一个 VPN 连接时会**同时删除绑定的 VPN 网关**。
