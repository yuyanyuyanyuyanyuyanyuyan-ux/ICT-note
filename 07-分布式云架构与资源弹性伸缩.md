---
课程: 华为云 HCCDA-Tech Essentials 云技术精髓入门级开发者认证
章节: 7. 分布式云架构与资源弹性伸缩
周次: 第一周
日期: 2026-09-11
标签: [华为云, ICT大赛, 云赛道]
---

# 7 分布式云架构与资源弹性伸缩

> 一句话概括：**分布式云把"一朵云"从中心机房延伸到业务现场（边缘 / 本地），并提供一致的体验；弹性伸缩（AS）则让算力像弹簧一样随业务负载自动"伸"与"缩"，与负载均衡（ELB）配合即构成云上最经典的弹性架构。**

## 学习目标

- 理解集中式云面临的挑战，掌握**分布式云**的定义、核心理念与价值。
- 说清华为云分布式云的**四大创新**与典型产品组合（IES、CloudPond、UCS 等）。
- 掌握**工作负载调度**的两大利器：负载均衡 **ELB** 与容器编排 **Kubernetes / CCE**。
- 掌握**弹性伸缩 AS** 的定义、相关概念（伸缩组 / 伸缩配置 / 伸缩策略 / 伸缩活动 / 冷却时间 / 伸缩带宽）。
- 熟悉 AS 的**产品架构、优势、创建流程**与三类伸缩策略（告警 / 定时 / 周期）。
- 理解 **AS 与 ELB 配合**为什么能同时实现"弹性"与"高可用"。
- 能对典型场景（论坛、电商大促、视频直播）选择正确的伸缩策略。

---

## 7.1 分布式云架构

### 7.1.1 为什么需要分布式云

集中式建设的云（所有资源集中在一个或少数几个大型数据中心）在行业全面云化时暴露出多重痛点：

- **组织多层级**：政府行业省、市、区、县的资源需要**统一治理**；
- **数据本地留存**：金融行业既要满足**数据隔离、本地留存**要求，又要统一分析；
- **极低时延**：汽车、工业场景需要 OT 与 IT 高效协同，OT 业务对时延极其敏感；
- **资源多地域、场景多样化、业务多类型**：单一中心云难以覆盖全部业务位置。

一句话：**"云要跑到业务需要的地方去"**——这就是分布式云要解决的问题。

### 7.1.2 分布式云的定义

> **分布式云**是指将公有云的服务能力，**从中心云延伸到边缘、本地等业务所需位置**，并通过统一的管理、一致的架构与运维体系，让用户在任何位置都能获得**一致的云服务体验**。

关键词：**云延伸到业务所需位置 + 全域统一调度 + 一致体验**。

### 7.1.3 华为云分布式云的四大创新

华为云基于"**分布式云 × 云原生**"理念，提出云基础设施新范式，通过四大创新让云延伸到业务所需位置：

| 创新 | 名称 | 关键点 |
| --- | --- | --- |
| 1 | **一朵分布式云** | 云基础设施从中心延伸至边缘，覆盖业务全场景；通过**瑶光分布式云操作系统**实现全域调度 |
| 2 | **一张分布式网络** | LocalGateway、EdgeGateway、安全 VPC 三大关键技术，实现企业多种组网环境高效安全互通 |
| 3 | **一致运行环境** | 中心与边缘基于一致的**擎天架构**，应用"一次开发适配、任意位置部署"，任一地部署、全网可访问 |
| 4 | **一套管理体系** | 通过 **EPS + IAM** 统一权限视图，通过 **RMS + CES** 统一资源与运维监控视图，实现精细化治理 |

### 7.1.4 典型产品组合（了解）

- **智能边缘小站 IES**：将云能力下沉到客户机房/近场，降低时延；
- **CloudPond**：标准化整机柜交付，把华为云服务延伸到客户本地；
- **UCS（分布式云原生服务）**：统一接入华为云、自建 IDC、第三方云的 Kubernetes 集群，实现多云多集群统一管理、一致体验。

### 7.1.5 分布式云的价值

- **就近处理**：低时延、满足数据本地留存与合规；
- **统一治理**：一套管理体系管多层级、多地域资源；
- **一致体验**：应用一次开发、多位置部署，运维与开发体验不割裂；
- **业务连续**：配合多 AZ / 多 Region / 多活架构，抵御主机故障、机房中断与自然灾害。

---

## 7.2 工作负载调度（负载均衡与容器编排）

分布式云解决了"资源在哪里"的问题；**工作负载调度**解决"请求/任务交给谁处理"的问题。两大核心手段：

- **弹性负载均衡 ELB**：把访问流量按策略分发到后端多台服务器；
- **容器编排（Kubernetes / CCE）**：把容器化的工作负载调度到集群节点上运行。

### 7.2.1 弹性负载均衡 ELB

**定义**：弹性负载均衡（Elastic Load Balance，ELB）是将访问流量根据分配策略分发到后端多台服务器的**流量分发控制服务**。它通过流量分发扩展应用系统的对外服务能力，并通过消除单点故障提升可用性。

**产品架构三部分**：

| 组件 | 作用 |
| --- | --- |
| **负载均衡器** | 接收客户端流量，并将请求转发到一个或多个可用区中的后端服务器 |
| **监听器** | 用配置的协议和端口检查客户端连接请求，按分配策略转发到后端服务器组 |
| **后端服务器** | 监听器绑定后端服务器组，组内可含一台或多台后端服务器，可配置转发权重 |

要点补充：

- 可以为**后端服务器**配置转发权重，但**不能为后端服务器组**配置权重；
- 开启**健康检查**后，异常的服务器会被自动屏蔽，恢复正常后自动被纳回服务；
- 会话保持可让同一客户端的请求固定转发到同一台后端服务器。

**优势**：高性能（集群支持最高 1 亿并发连接）、高可用（多可用区同城双活容灾）、灵活扩展（与 AS 无缝集成）、简单易用（快速部署、多协议多算法可选）。

**配置流程**：创建负载均衡器 → 配置监听器 → 添加 ECS 作为后端服务器。

**典型应用场景**：

- **高访问量业务流量分发**：门户网站、移动应用市场；
- **ELB + AS 潮汐业务弹性**：电商"双 11 / 618"，实例随业务自动增减并自动加入/移出后端组；
- **消除单点故障**：多后端 + 健康检查，故障机自动屏蔽。

### 7.2.2 容器编排（Kubernetes / CCE）

容器把应用及其依赖打包成**镜像**，而**容器编排**负责在多台节点上调度、扩缩、自愈这些容器。华为云的 **CCE（云容器引擎）** 提供企业级 Kubernetes 集群（详见第 8 课）。

容器编排承担的工作负载调度能力包括：**资源调度、服务发现、动态伸缩、滚动升级、故障自愈**。调度器会根据节点资源、亲和性/反亲和性等策略，把 Pod 放到最合适的节点上运行。

---

## 7.3 资源弹性伸缩（AS）

### 7.3.1 AS 的定义

> **弹性伸缩（Auto Scaling，简称 AS）** 是根据用户的业务需求，通过设置**伸缩规则**来自动增加/缩减业务资源：业务增长时自动增加 ECS 实例或带宽，业务下降时自动缩减，从而**保证业务能力、节约成本**。AS 支持自动调整弹性云服务器（ECS）和带宽资源。

### 7.3.2 AS 的相关概念（重点）

| 概念 | 含义 |
| --- | --- |
| **伸缩组** | 具有相同应用场景的**实例的集合**，是启停伸缩策略和进行伸缩活动的基本单位 |
| **伸缩配置** | 伸缩组内实例（ECS）的**模板**，定义规格数据：云服务器类型、vCPU、内存、镜像、磁盘、登录方式等 |
| **伸缩策略** | 触发伸缩活动的方式，规定**触发条件**与**执行操作**；满足条件时系统自动触发一次伸缩活动 |
| **伸缩活动** | 伸缩组中**增加或减少实例**的过程；目的：使"当前实例数"与"期望实例数"保持一致 |
| **冷却时间** | 每次伸缩活动完成后系统开始计算的时间窗；**冷却时间内拒绝告警策略触发的伸缩活动**（定时/周期策略不受限制，但会重启计时），单位为秒 |
| **伸缩带宽** | 根据伸缩带宽策略自动调整带宽资源；**仅支持按需购买的 EIP 带宽和共享带宽**，不支持包年包月带宽 |

**冷却时间举例**：冷却时间设为 300 秒，定时策略 10:32 有伸缩活动，10:30 告警触发的活动结束 → 10:30–10:35 内拒绝新告警触发，但**不拒绝** 10:32 的定时策略；若 10:36 定时活动结束，则冷却时间为 10:36–10:41。

> 口诀：**冷却时间只"卡"告警策略，不"卡"定时/周期策略。**

### 7.3.3 AS 的产品架构

通过**伸缩控制**实现对 ECS 实例与带宽的伸缩：

- **伸缩控制**：配置策略设置指标阈值 / 伸缩活动执行时间，通过**云监控**判断指标是否达到阈值，通过**定时调度**实现伸缩控制；
- **配置策略**：根据业务需求配置三类策略；
  - **告警策略**：可配置 CPU、内存、磁盘、入网流量等监控指标；
  - **定时策略**：通过配置触发时间触发；
  - **周期策略**：通过配置重复周期、触发时间、生效时间触发。

云监控服务监控到告警策略中的指标达到阈值即触发伸缩活动；到达触发时间时，定时/周期策略触发伸缩活动。

### 7.3.4 AS 的优势

- **自动调整资源**：业务增长自动增加实例/带宽，业务下降自动缩容，保障业务平稳运行；
- **加强成本管理**：按需使用计算与带宽，减少人为调整损耗，最大化节约成本；
- **提高可用性**：确保容量匹配当前流量；**与负载均衡器结合**后，伸缩组自动为新实例绑定监听器，流量自动分发到组内所有实例；
- **提高容错能力**：检测实例运行状况，用新实例替换状况不佳的实例。

### 7.3.5 AS 的创建流程（三步）

| 步骤 | 操作 | 关键点 |
| --- | --- | --- |
| 第一步 | **创建伸缩配置** | ①用已有 ECS 快速创建（规格参数默认与所选 ECS 一致）；②用新模板创建全新配置（可自定义规格） |
| 第二步 | **创建伸缩组** | 配置多可用区扩展策略、最大/期望/最小实例数、实例移除策略、健康检查等 |
| 第三步 | **创建伸缩策略** | 配置**策略类型**与**冷却时间** |

伸缩组配置要点：

- **多可用区扩展策略**：选择两个及以上可用区时才需要配置；
- **最大 / 最小实例数**：伸缩组内 ECS 数量的上下限；**期望实例数**介于两者之间；
- **实例移除策略**：自动移除实例时，若组内存在不属于当前配置可用区的实例，**优先移除**这些实例。

### 7.3.6 伸缩策略怎么选

| 策略类型 | 适用场景 | 说明 |
| --- | --- | --- |
| **告警策略** | 业务负载**难以预测** | 依据实时监控数据（如 CPU 使用率）触发，完成后进入冷却时间 |
| **定时策略** | 负载**可预测的固定时刻** | 如每天固定时间扩容；不受冷却时间限制 |
| **周期策略** | **有规律重复**的负载 | 配置重复周期、触发时间、生效时间 |

### 7.3.7 AS 与 ELB 配合

- 伸缩组与 ELB 结合后，**新加入的实例自动绑定负载均衡监听器**，被移除的实例自动从后端组移除；
- 流量通过监听器自动分发到组内所有实例 → 在**弹性**的基础上进一步提高**可用性**；
- ELB 侧的健康检查会屏蔽故障后端，AS 侧则补足实例数量 → 双保险。

### 7.3.8 AS 的应用场景

- **论坛网站**：访问流量大、负载难预测，按 CPU/内存使用率动态调整服务器数量；
- **电商大促**：促销期间**定时**增加实例与带宽，保证活动顺利进行；
- **视频直播**：每天固定时段（如 14:00–16:00）**周期**扩容，平稳承载高峰。

---

## 核心概念速查表

| 术语 | 含义 |
| --- | --- |
| 分布式云 | 云能力从中心延伸到边缘/本地，统一管理、一致体验 |
| 瑶光 | 华为云分布式云操作系统，实现全域调度 |
| 擎天 | 华为云软硬协同架构，保证中心与边缘一致运行环境 |
| ELB | 弹性负载均衡，按策略把流量分发到多台后端服务器 |
| 监听器 | ELB 中检查并转发客户端请求的组件 |
| 后端服务器组 | 监听器绑定的服务器集合，可配置成员权重 |
| AS | 弹性伸缩，按策略自动增减 ECS/带宽资源 |
| 伸缩组 | 实例集合，伸缩活动的基本单位 |
| 伸缩配置 | 伸缩组内实例的创建模板 |
| 伸缩策略 | 规定触发条件与执行操作的规则 |
| 冷却时间 | 伸缩活动完成后拒绝告警策略触发的计时窗口（秒） |
| 伸缩带宽 | 自动调整带宽，仅支持按需 EIP/共享带宽 |
| 期望实例数 | 伸缩组期望运行的实例数量，介于最小与最大之间 |

---

## 考点 / 易错点

- **冷却时间只约束告警策略**：定时、周期策略触发的伸缩活动不受冷却时间限制（但会重新开始计算冷却时间）。
- **伸缩带宽不支持包年包月**：只调整按需购买的 EIP 带宽和共享带宽。
- **权重配置对象**：后端**服务器**可配权重，后端**服务器组**不可配权重。
- **多可用区扩展策略**：选择两个及以上可用区时才需要配置。
- **实例移除策略**：优先移除不属于当前配置可用区的实例。
- **ELB 三件套**：负载均衡器 → 监听器 → 后端服务器组，配置顺序不要记反。
- **AS 三步走**：伸缩配置 → 伸缩组 → 伸缩策略。
- 分布式云 ≠ 混合云：分布式云强调的是**一致的架构和管理体系**，而不仅是"公有 + 私有"的组合。

---

## 自测题

**1. 下列关于冷却时间的说法，正确的是（　）**
A. 冷却时间内，告警策略和定时策略都无法触发伸缩活动
B. 冷却时间内，告警策略触发的伸缩活动会被拒绝
C. 冷却时间的单位是分钟
D. 冷却时间只在创建伸缩组时设置一次，之后不可更改

> **答案：B。** 冷却时间只拒绝由**告警策略**触发的伸缩活动；定时/周期策略不受限制；单位为秒。

**2. 弹性伸缩（AS）创建流程的正确顺序是（　）**
A. 伸缩组 → 伸缩配置 → 伸缩策略
B. 伸缩策略 → 伸缩组 → 伸缩配置
C. 伸缩配置 → 伸缩组 → 伸缩策略
D. 伸缩配置 → 伸缩策略 → 伸缩组

> **答案：C。** 先有实例模板（伸缩配置），再建集合（伸缩组），最后配规则（伸缩策略）。

**3. ELB 的三个组成部分是（　）**
A. 负载均衡器、监听器、后端服务器组
B. 伸缩组、伸缩配置、伸缩策略
C. VPC、子网、安全组
D. 集群、节点、Pod

> **答案：A。**

**4. 关于伸缩带宽，下列说法正确的是（　）**
A. 支持调整包年包月带宽
B. 只支持调整按需购买的 EIP 带宽和共享带宽
C. 只能增大不能减小
D. 与 ECS 实例伸缩互斥，不能同时配置

> **答案：B。**

**5. 某视频直播网站每天 14:00–16:00 播出热门节目，需要在该时段自动扩容，最合适的策略类型是（　）**
A. 告警策略
B. 定时策略或周期策略
C. 无需策略，手动扩容
D. 只能使用冷却时间控制

> **答案：B。** 负载有明显规律、时间可预测，适合定时/周期策略；告警策略更适合负载难预测的场景。

**6.（多选）AS 与 ELB 结合后能带来哪些好处？**
A. 新加入的实例自动绑定负载均衡监听器
B. 流量自动分发到伸缩组内所有实例
C. 被移除的实例自动从后端服务器组移除
D. 不再需要健康检查

> **答案：ABC。** 健康检查仍然重要，AS 自身也依赖健康检查替换不健康实例。
