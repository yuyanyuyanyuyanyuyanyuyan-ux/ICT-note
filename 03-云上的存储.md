---
课程: 华为云 HCCDA-Tech Essentials 云技术精髓入门级开发者认证
章节: 3. 云上的存储
周次: 第一周
日期: 2026-09-11
标签: [华为云, ICT大赛, 云赛道]
---

# 3 云上的存储

> 一句话概括：**华为云把存储分成三件"利器"——云硬盘 EVS（块存储，给系统盘/数据盘）、对象存储 OBS（海量非结构化数据）、弹性文件服务 SFS（多主机共享的 NAS 文件存储）。**

## 学习目标

- 掌握**云硬盘 EVS**的定义、架构、优势、磁盘类型与性能指标、应用场景、磁盘模式与特性（共享、加密、备份、快照、三副本）。
- 掌握**对象存储 OBS**的定义、产品架构（桶/对象）、核心功能、优势、访问方式与应用场景。
- 掌握**弹性文件服务 SFS**的定义、相关概念（NFS/CIFS/文件系统）、优势、应用场景与配置流程。
- 能对比块存储 / 文件存储 / 对象存储三种类型，并能区分云硬盘的**备份**与**快照**。

---

## 5.1 云硬盘 EVS

### 5.1.1 定义

> 云硬盘（Elastic Volume Service，**EVS**）可以为弹性云服务器（ECS）或裸金属服务器（BMS）提供**高可靠、高性能、规格丰富、可弹性扩展的块存储服务**，可满足不同场景业务需求，适用于分布式文件系统、开发测试、数据仓库、高性能计算等场景。

### 5.1.2 产品架构

- 结构类似 PC 中的硬盘，**需挂载至 ECS/BMS 上使用，无法单独使用**；
- 可对已挂载云硬盘执行初始化、创建文件系统等操作，把数据**持久化**存储；
- 可通过**云硬盘备份和云硬盘快照**提高可靠性。

### 5.1.3 优势

| 优势 | 说明 |
| --- | --- |
| 规格丰富 | 多种规格，可挂载至 ECS 作数据盘/系统盘，按业务与预算选择 |
| 弹性扩展 | 单个云硬盘容量 **10 GB ≤ 容量 ≤ 32 TB**；最小扩容步长 **1 GB**；支持**平滑扩容**（无需暂停业务）；受存储容量总配额影响 |
| 安全可靠 | 系统盘/数据盘均支持数据加密；支持备份、快照等保护功能 |
| 实时监控 | 配合**云监控（Cloud Eye）**掌握云硬盘健康状态与运行状况 |

### 5.1.4 磁盘类型及性能

按性能划分（从高到低）：

| 类型 | 特点 / 适用 |
| --- | --- |
| **极速型 SSD** | 采用结合低时延拥塞控制算法的 **RDMA** 技术，单盘最大吞吐量达 **1000 MB/s**，极低单路时延；适用于需要超大带宽和超低时延的场景 |
| **超高 IO** | 超高性能，适合高吞吐、低时延工作负载 |
| **通用型 SSD** | 高性价比，用于高吞吐、低时延的企业办公 |
| **高 IO** | 可用于一般访问的工作负载 |
| **普通 IO**（上一代） | 不常访问的工作负载；大容量、读写速率中等、事务处理较少；不建议用于系统盘 |

**性能主要指标**：

- **IOPS**：云硬盘每秒进行读写的操作次数；
- **吞吐量**：每秒成功传送的数据量；
- **IO 读写时延**：连续两次读写操作所需的最小时间间隔。

> 吞吐量与 IOPS 的关系：当最大 IOPS 或最大吞吐量**有一项达到最大值**时，性能达到最大，另一项无法继续上升。小数据块（4 KB、8 KB）可达到**最大 IOPS**；大数据块（≥ 16 KB）可达到**最大吞吐量**。

**各类型对应场景**：

- 极速型 SSD：高性能数据库（Oracle、SQL Server、ClickHouse）、AI 场景；
- 超高 IO：超大带宽读写密集型、转码类、I/O 密集型（NoSQL、Oracle、SQL Server、PostgreSQL）、时延敏感（Redis、Memcache）；
- 通用型 SSD：企业办公、大型开发测试、转码类、Web 服务器日志、容器等高性能系统盘；
- 高 IO：普通开发测试等一般工作负载；
- 普通 IO：日常办公、轻载型开发测试。

### 5.1.5 磁盘模式

按是否支持高级 SCSI 命令划分，购买时配置，**购买后无法修改**：

| 模式 | 说明 |
| --- | --- |
| **VBD**（Virtual Block Device） | 默认类型；只支持简单的 SCSI 读写命令 |
| **SCSI**（Small Computer System Interface） | 支持 **SCSI 指令透传**，允许 OS 直接访问底层存储介质；支持更高级的 SCSI 命令 |

选择建议：

- **SCSI 磁盘**：BMS **仅支持**使用 SCSI 磁盘，用作系统盘和数据盘；
- **SCSI 共享盘**：使用共享盘时需结合分布式文件系统或集群软件（多数集群需 SCSI 锁，如 Windows MSCS、Veritas VCS、CFS 集群）；若挂载至 ECS，需结合**云服务器组的反亲和性**一同使用 SCSI 锁才生效。

### 5.1.6 特性概览

| 特性 | 要点 |
| --- | --- |
| **共享云硬盘** | 一个云硬盘可被多个 ECS/BMS 并发读写；多挂载点、高并发、高性能、高可靠；用于需集群、HA 能力的关键企业应用。⚠️ 必须搭建共享文件系统或集群管理系统，直接挂载多台无法共享且有数据覆盖风险；一块共享盘**最多挂载 16 台**云服务器 |
| **云硬盘加密** | 授权 EVS 访问 **KMS（密钥管理服务）** 使用加密功能，系统为用户创建主密钥用于加密云硬盘 |
| **云硬盘备份** | 在线备份、无需关闭云服务器；可通过任意时刻的备份副本恢复数据；可设置备份策略自动备份 |
| **云硬盘快照** | 云硬盘数据在快照创建时的完整拷贝或镜像，重要数据容灾手段；数据丢失时可恢复到快照创建时间点 |
| **三副本机制** | 一份数据被复制成 **3 个副本**存放在不同节点，并保证副本间数据强一致；任一磁盘故障仍可继续提供存储服务 |
| **副本重建机制** | 存储系统检测到硬件故障时自动启动数据重建；副本分散存储，多节点并行重建，避免单节点性能瓶颈 |

**快照 vs 备份**（易混淆，重点记）：

1. 两者都是重要的数据容灾手段，但**存储方案不同**；
2. **快照**数据与云硬盘数据存储在一起（支持快速备份和恢复）；**备份**数据存储在 **OBS** 中（可在云硬盘存储损坏情况下恢复）；
3. **快照当前不支持设置自动创建**；**备份支持设置自动创建**（可通过备份策略自动执行）。

### 5.1.7 配置流程

创建云硬盘 → 挂载到云服务器/BMS → 初始化（创建文件系统）→ 正常使用。

> ⚠️ 再次强调：**云硬盘不能单独使用**，必须挂载到 ECS 或 BMS 上初始化后才能正常使用。

---

## 5.2 对象存储服务 OBS

### 5.2.1 定义

> 对象存储服务（Object Storage Service，**OBS**）是一个**基于对象的海量存储服务**，可为客户提供海量、安全、高可靠、低成本的数据存储能力。

### 5.2.2 产品架构

OBS 的基本组成是**桶（Bucket）**和**对象（Object）**。

- **桶**：OBS 中存储对象的**容器**，每个桶有自己的存储类别、访问权限、所属区域等属性；用户在互联网上通过**桶的访问域名**来定位桶。
- **对象**：OBS 中数据存储的**基本单位**，是文件数据与其相关属性信息的集合体，包括 **Key、Metadata、Data** 三部分：
  - **Key**：键值，即对象名称，为 UTF-8 编码、长度 > 0 且 ≤ 1024 的字符序列；一个桶里每个对象的 Key 必须唯一；
  - **Metadata**：元数据（描述信息），以 Key-Value 形式上上传。分**系统元数据**（OBS 自动产生，如 Date、Content-length、Last-modify、Content-MD5）与**用户元数据**（用户上传时指定）；
  - **Data**：文件的数据内容。

**模拟文件夹**：OBS 没有文件系统的文件夹概念，通过在对象名称中增加 "/" 来模拟（如 `test/123.jpg`），实际对象 Key 仍是 `test/123.jpg`。

**存储类别**：上传对象时可指定，不指定则默认与桶一致，上传后可修改。

**认证鉴权（AK/SK）**：

- 使用 **Access Key ID（AK）/ Secret Access Key（SK）** 加密方式验证请求发送者身份；
- 支持**永久 AK/SK**（在"我的凭证"页面创建）与**临时 AK/SK + securitytoken**；
- 临时 AK/SK 和 securitytoken 有效期 **15 分钟至 24 小时**，遵循权限最小化原则；若未使用 securitytoken 会返回 **403 错误**。

### 5.2.3 OBS 的功能

| 功能 | 说明 |
| --- | --- |
| 服务端加密 | 上传时在服务端加密成密文存储；下载时先在服务端解密为明文再提供 |
| 防盗链 | 支持访问白名单/黑名单；通过 HTTP 表头字段 **Referer** 检测请求来源是否与名单匹配 |
| 多版本控制 | 桶中保留多个版本的对象，误操作/故障时快速恢复；默认**不开启**（同名对象会被覆盖） |
| 自定义域名绑定 | 网站文件迁移到 OBS 且不想改链接时使用：创建桶并上传文件 → 控制台绑定自定义域名 → （可选）访问量 > 10 万时配 CDN → 在域名服务器添加 **CNAME** 规则映射到桶域名 |
| 跨区域复制 | 源桶数据自动、**异步**复制到不同区域的目标桶（**须同一个账号**，暂不支持跨账号）；用于合规性、减少延迟、数据复制、备份容灾、易维护；复制对象与源对象具有相同名称和元数据 |
| 生命周期管理 | 定时删除桶中对象或定时转换对象存储类别（减少租赁费用）；适用于周期性日志、低频化文档、归档数据、一次性批量删除等场景 |
| 权限管理 | 默认资源（桶和对象）均为**私有**；通过编写访问策略向其他账号或 IAM 用户授予权限（可开放为公共读等） |

> 跨区域复制会复制的对象：新上传的对象（归档存储对象除外）、有更新的对象、桶中的历史对象（需开启"同步历史对象"，归档存储对象除外）。

### 5.2.4 优势（五点）

- **数据稳定，业务可靠**：华为手机云相册后端即使用 OBS；数据持久性高达 **99.9999999999%**（12 个 9），业务连续性高达 **99.995%**；
- **多重防护，授权管理**：通过可信云认证；支持多版本控制、服务端加密、防盗链、VPC 网络隔离、访问日志审计及细粒度权限控制；
- **千亿对象，千万并发**：智能调度和响应，优化数据访问路径，提供千万级并发、超高带宽、稳定低时延访问；
- **简单易用，便于管理**：支持标准 REST API、多版本 SDK 和数据迁移工具；无需事先规划存储容量，可线性无限扩展；
- **数据分层，按需使用**：按量计费和包年包月；支持标准、低频访问、归档数据独立计量计费，降低存储成本。

### 5.2.5 访问方式（五种）

| 方式 | 说明 |
| --- | --- |
| 管理控制台 | 网页形式，通过账号名&密码或 IAM 登录认证身份 |
| SDK & API | SDK 是对 REST API 的封装；REST API 便于从 Web 应用访问 OBS |
| OBS Browser+ | Windows 系统上的对象存储管理工具，图形化界面，通过 AK/SK 鉴权 |
| obsutil | 命令行工具，适合批量处理、自动化任务 |
| obsfs | 基于 **FUSE** 的文件系统工具，主要用于将**并行文件系统挂载至 Linux 系统** |

### 5.2.6 应用场景

- **大数据分析类**：海量数据存储分析、历史数据明细查询、海量行为日志分析、公共事务分析统计；
- **静态网站托管**：低成本、高可用、可按流量自动扩展；结合 **CDN** 和 **ECS** 构建动静态分离的网站/应用系统（静态数据存 OBS）；
- **在线视频点播**：结合媒体处理 **MPC**、内容审核 **Moderation**、内容分发网络 **CDN** 搭建（作为点播源站）；
- **备份归档**：通过同步客户端、主流备份软件、云存储网关或数据快递服务 **DES** 备份至 OBS；用生命周期功能自动转换存储类别；
- **企业云盘（网盘）**：配合 ECS、ELB、RDS、云硬盘备份 **VBS** 提供高并发、高可靠、低成本存储，容量随数据量自动扩容。

---

## 5.3 弹性文件服务 SFS

### 5.3.1 定义

> 弹性文件服务（Scalable File Service，**SFS**）提供**按需扩展的高性能文件存储（NAS）**，可为云上多个弹性云服务器（ECS）、容器（CCE & CCI）、裸金属服务器（BMS）提供**共享访问**。

### 5.3.2 相关概念

| 概念 | 含义 |
| --- | --- |
| **NFS**（Network File System） | 网络文件系统，用于分散式文件系统的协议，通过网络让不同机器、不同操作系统彼此分享数据 |
| **CIFS**（Common Internet File System） | 通用 Internet 文件系统，是公共/开放的 **SMB** 协议版本，由微软使用；可实现 Windows 系统主机之间的网络文件共享 |
| 文件系统 | 通过标准 NFS 和 CIFS 协议提供文件存储服务，用于网络文件远程访问；用户创建挂载地址后可在多个云服务器挂载，通过标准 **POSIX** 接口访问 |

### 5.3.3 优势（四点）

- **弹性扩展**：不中断应用的情况下增减文件系统容量，一键式操作；
- **高性能、高可靠性、低成本**：性能随容量增加而提升，保障数据高持久度；
- **无缝集成**：同时支持 **NFS 和 CIFS** 协议，兼容 **SMB 2.0/2.1/3.0**，Windows 客户端可轻松访问；
- **操作简单、便于管理**：界面简单易用，轻松快捷创建和管理文件系统。

### 5.3.4 应用场景

**SFS 常见应用场景**：

- **HPC 场景**：仿真实验、生物制药、基因测序、图像处理、科学研究、气象预报等，提供高带宽、大容量共享存储；
- **媒体处理场景**：流媒体、归档、编辑、转码、内容分发、视频点播；高清编辑向 30–40 层编辑转型，需稳定的高带宽、低时延性能；
- **内容管理和 Web 服务**：为网站、主目录、在线发行、存档等提供共享文件存储；
- **大数据和分析应用**：最高 **10 GBps 聚合带宽**，处理卫星影像等超大数据文件。

**SFS Turbo 常见应用场景**：提供完全托管的共享文件存储，可弹性伸缩至 **320 TB**，为海量小文件、低延迟高 IOPS 型应用提供支持（如高性能网站、日志存储、压缩解压、DevOps、企业办公、容器应用）。

- 高性能网站：为多个 Web Server 提供共享网站源码目录，低延迟高 IOPS 并发共享访问；
- 日志存储：为多个业务节点提供共享日志输出目录；
- DevOps：将开发目录共享到多个 VM 或容器；
- 企业办公：存放办公文档，高性能共享访问。

### 5.3.5 配置流程

1. **创建文件系统**：可创建 **SFS 容量型** 和 **SFS Turbo** 两种类型；在多个云服务器中挂载使用，实现文件系统共享访问；
2. **挂载文件系统**：创建后用云服务器挂载该文件系统，以实现多个云服务器共享使用。

- **Linux 挂载 NFS**：以 root 用户登录 Linux ECS → 安装 NFS 客户端 → 挂载；
- **Windows 挂载 CIFS**：通过 CIFS 协议实现 Windows 系统共享。

---

## 核心概念速查表

| 术语 | 含义 |
| --- | --- |
| EVS | 云硬盘，为 ECS/BMS 提供块存储服务，不能单独使用 |
| OBS | 对象存储服务，基于对象的海量存储 |
| SFS | 弹性文件服务，按需扩展的高性能文件存储（NAS） |
| 桶 / 对象 | OBS 中存储对象的容器 / 数据存储的基本单位（Key+Metadata+Data） |
| AK / SK | 访问密钥 ID / 私有访问密钥，用于对请求加密签名 |
| IOPS / 吞吐量 / 时延 | 每秒读写操作次数 / 每秒成功传送数据量 / 连续两次读写最小时间间隔 |
| VBD / SCSI 磁盘模式 | 默认，仅简单 SCSI 读写 / 支持 SCSI 指令透传 |
| 三副本机制 | 一份数据复制成 3 个副本存放在不同节点，保证强一致 |
| 快照 / 备份 | 与云硬盘数据存在一起（不支持自动创建）/ 存储在 OBS 中（支持自动创建）|
| NFS / CIFS | 网络文件系统协议（Linux）/ 通用 Internet 文件系统，SMB 协议（Windows） |
| SFS Turbo | SFS 的 Turbo 版本，可伸缩至 320TB，适合海量小文件高 IOPS |

## 考点 / 易错点

- **云硬盘不能单独使用**，必须挂载到 ECS/BMS 并初始化；容量范围 **10 GB – 32 TB**，最小扩容步长 1 GB。
- **快照 vs 备份**：存储位置不同（快照与云硬盘同存；备份在 OBS 中）；快照**不支持自动创建**、备份**支持自动创建**。
- **共享云硬盘**：必须搭共享文件系统/集群管理系统，否则有数据覆盖风险；最多挂载 **16 台**云服务器；结合 ECS 使用时需用云服务器组反亲和性。
- **磁盘模式购买后不可修改**；BMS 只能用 **SCSI** 磁盘。
- **OBS**：对象 = Key + Metadata + Data；默认桶不开启多版本，同名对象会被覆盖；跨区域复制**须同账号**、**异步**；未使用 securitytoken 返回 403。
- **OBS 持久性 12 个 9（99.9999999999%），业务连续性 99.995%**。
- **SFS 同时支持 NFS 和 CIFS**，兼容 SMB 2.0/2.1/3.0；SFS 提供 NAS 文件存储；SFS Turbo 可伸缩至 320TB。
- **访问方式**别记混：OBS Browser+（Windows 图形化）、obsutil（命令行）、obsfs（基于 FUSE 挂载到 Linux）。

## 自测题

1. 云硬盘 EVS 的容量范围是多少？最小扩容步长是多少？为什么云硬盘不能单独使用？
2. 云硬盘的性能指标有哪三个？小数据块和大数据块分别能达到哪项最大值？
3. 简述云硬盘**快照**与**备份**的三点区别。
4. OBS 的"对象"由哪三部分组成？OBS 的存储持久性和业务连续性分别是多少？
5. OBS 跨区域复制有哪些限制？未使用 securitytoken 会返回什么错误？
6. SFS 支持哪两种协议？分别对应什么操作系统？SFS 与 SFS Turbo 有何区别？

<details>
<summary>参考答案</summary>

1. 容量范围 **10 GB ≤ 容量 ≤ 32 TB**；最小扩容步长 **1 GB**。云硬盘是块存储服务，只提供存储空间，必须挂载到 ECS 或 BMS 上初始化、创建文件系统后才能使用，无法单独使用。
2. **IOPS、吞吐量、IO 读写时延**。小数据块（4 KB、8 KB）可达到最大 IOPS；大数据块（≥ 16 KB）可达到最大吞吐量。
3. ① 均为数据容灾手段但存储方案不同；② 快照数据与云硬盘数据存储在一起，备份数据存储在 OBS 中（可在云硬盘损坏时恢复）；③ 快照当前不支持设置自动创建，备份支持设置自动创建。
4. 由 **Key（对象名称）、Metadata（元数据）、Data（数据内容）** 三部分组成。数据持久性高达 **99.9999999999%**（12 个 9），业务连续性高达 **99.995%**。
5. 源桶和目标桶必须属于**同一个账号**，暂不支持跨账号复制；复制是**异步**的。未使用 securitytoken 会返回 **403 错误**。
6. 支持 **NFS**（网络文件系统，Linux）和 **CIFS**（通用 Internet 文件系统，是开放的 SMB 版本，Windows）两种协议。SFS Turbo 为用户提供完全托管的共享文件存储，可弹性伸缩至 **320 TB**，具备高可用性和持久性，为海量小文件、低延迟高 IOPS 型应用提供支持。

</details>
