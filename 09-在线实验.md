---
课程: 华为云 HCCDA-Tech Essentials 云技术精髓入门级开发者认证
章节: 9. 在线实验
周次: 第一周
日期: 2026-09-11
标签: [华为云, ICT大赛, 云赛道]
---

# 9 在线实验

> 一句话概括：**5 个沙箱实验把前 8 课的理论"跑"一遍——从一台 ECS 的出生、到存储/网络的搭建、再到数据库的连接与弹性架构的自动伸缩，完整走通一条云上业务链条。**

## 学习目标

- 熟练通过控制台完成**计算、存储、网络、数据库**类基础服务的实操。
- 掌握 **ECS 购买、镜像使用、云硬盘挂载、OBS 桶操作、SFS 挂载** 等关键步骤。
- 掌握 **VPC / 子网 / 安全组 / 网络 ACL** 的配置与差异。
- 能独立**创建并连接 RDS for MySQL 实例**。
- 能基于 **AS** 搭建一套具备自动伸缩能力的基础弹性架构并做稳定性测试。
- 建立"先规划网络 → 再创建计算/存储/数据库 → 最后做弹性与验证"的实验方法论。

---

## 实验通用须知

- **实验环境**：华为云在线实验（沙箱）环境，通常会发放**临时账号 + 代金券/免费额度**；务必在实验结束前**释放资源**，避免欠费或额度耗尽。
- **通用步骤套路**：登录实验账号 → 按需切换区域（Region）→ 规划 VPC/子网 → 创建资源 → 验证连通性 → 释放资源。
- **网络优先原则**：涉及多台 ECS 的实验，**先建好 VPC 与子网、安全组**，再创建实例。
- **常见踩坑**：安全组入方向未放行端口导致连不上；ECS 未绑定 EIP 导致无法从外网访问；镜像/规格选错导致费用或兼容问题。
- **文档习惯**：实验全程截图或记录关键参数（实例 ID、IP、挂载地址），写实验报告时直接用。

---

## 9.1 计算类基础服务实践（ECS / IMS / AS）

### 实验目的

- 亲手完成一台弹性云服务器（**ECS**）的购买、登录与基础使用；
- 理解**镜像服务（IMS）**的作用：用私有镜像批量创建 ECS；
- 初步体验**弹性伸缩（AS）**：让实例数量随负载自动变化。

### 涉及服务

| 服务 | 作用 |
| --- | --- |
| **ECS** | 弹性云服务器，由 CPU、内存、操作系统、云硬盘组成的基础计算组件 |
| **IMS** | 镜像服务，管理镜像生命周期（公共/私有/共享/市场镜像） |
| **AS** | 弹性伸缩，按策略自动增减 ECS 实例 |
| **VPC/安全组** | 为 ECS 提供网络与访问控制 |
| **EIP** | 弹性公网 IP，提供公网访问入口 |

### 操作要点

1. **准备**：确认有可用账号与余额（或实验代金券）。
2. **购买 ECS**（核心步骤）：
   - **基础配置**：计费模式（按需 / 包周期 / 竞价）、区域和可用区（就近选择降低时延）、**CPU 架构**（X86 复杂指令集 CISC / 鲲鹏 精简指令集 RISC）、规格与镜像；
   - **网络配置**：选择**子网**（DHCP 自动分配或手工指定 IP）、选择合适的**安全组**、扩展网卡；
   - 其余：系统盘/数据盘、登录方式（密码 / 密钥对）、实例数量。
3. **访问 ECS**：
   - 登录 **Windows ECS**：远程桌面（RDP，默认端口 3389）；
   - 登录 **Linux ECS**：SSH（默认端口 22）；
   - 控制台还提供 **VNC** 方式登录。
4. **使用 ECS**：体验重装/切换操作系统、**变更规格**、重置密码等操作。
5. **镜像实践（IMS）**：
   - 选择公共镜像创建实例；将已配置好的 ECS 制作为**私有镜像**；
   - 用该私有镜像**批量创建 ECS**，验证环境一致；
   - 了解镜像的共享、复制（区域内）、导出到 OBS 等能力。
6. **弹性体验（AS）**：将 ECS 放入伸缩组，尝试配置一条伸缩策略并观察实例数量变化（详见 9.5）。

### 结论

- ECS 是"综合性产品"：计算（CPU/内存）+ 存储（EVS）+ 网络（VPC/EIP/安全组）+ 镜像（IMS）+ 监控（CES）的组合。
- 用**私有镜像批量发放**是快速部署一致环境的利器。
- ECS 的"弹性伸缩"优势需要借助 **AS** 才能发挥（ESC 自身可伸缩规格，数量伸缩靠 AS）。

---

## 9.2 存储类基础服务实践（EVS / OBS / SFS）

### 实验目的

- 掌握**云硬盘（EVS）**的创建、挂载与初始化；
- 掌握**对象存储（OBS）**桶与对象的管理；
- 掌握**弹性文件服务（SFS）**的创建与挂载（多台 ECS 共享）。

### 涉及服务

| 服务 | 类型 | 特点 |
| --- | --- | --- |
| **EVS** | 块存储 | 挂载到 ECS/BMS 使用，**不能单独使用**；需初始化、创建文件系统 |
| **OBS** | 对象存储 | 基于对象的海量存储，基本组成 = **桶 Bucket + 对象 Object**，通过域名/API/SDK 访问 |
| **SFS** | 文件存储（NAS） | 支持 **NFS / CIFS** 协议，多台 ECS/容器共享 |

### 操作要点

1. **EVS 云硬盘**：
   - 创建云硬盘（容量范围 **10 GB ≤ 容量 ≤ 32 TB**，扩容步长最小 1 GB）；
   - **挂载**到目标 ECS → 登录 ECS → **初始化**（Windows 初始化 / Linux 分区格式化）→ 创建文件系统 → 挂载到目录；
   - 体验**扩容**（支持平滑扩容，无需暂停业务）、快照与备份。
2. **OBS 对象存储**：
   - 创建**桶 Bucket**（注意区域、存储类别、访问权限）；
   - **上传/下载对象**（对象 = Key + Metadata + Data）；体会"通过 `/` 模拟文件夹"的机制；
   - 了解访问方式：管理控制台、SDK、工具、REST API；认证鉴权用 **AK/SK**（支持永久与临时）。
3. **SFS 弹性文件服务**：
   - 创建文件系统（**SFS 容量型** 或 **SFS Turbo**）；
   - **挂载到 Linux（NFS）**：
     - 安装 NFS 客户端 → `nslookup 文件系统域名`（SFS Turbo 可跳过）→ `mkdir 本地路径` → `mount -t nfs -o vers=3,timeo=600 挂载地址 本地路径`；
     - 说明：`-t` 指定文件系统类型，`nfs` 表示网络文件共享；当前文件系统仅支持 **NFSv3** 挂载到 Linux。
   - **挂载到 Windows（CIFS）**：资源管理器 → 映射网络驱动器 → 输入 `\\文件系统域名\路径`；
   - 在**多台 ECS** 上挂载同一文件系统，验证共享访问。

### 结论

- 三种存储对应三种访问需求：**块（EVS，给一台机器当盘用）、对象（OBS，海量文件/备份/静态资源）、文件（SFS，多机共享目录）**。
- EVS 必须挂载并初始化后才能用；OBS 无容量规划烦恼，可线性无限扩展；SFS 用标准协议实现多机共享。

---

## 9.3 网络类基础服务实践（VPC / 安全组 / 网络 ACL）

### 实验目的

- 掌握**虚拟私有云（VPC）**的创建与子网划分；
- 理解并配置**安全组**与**网络 ACL**，实现更精细的访问控制；
- 验证不同 VPC / 不同子网之间的连通性规则。

### 涉及服务

| 服务 | 作用范围 | 说明 |
| --- | --- | --- |
| **VPC** | 网络环境 | 由**私网网段 + 路由表 + 至少一个子网**组成；VPC 之间逻辑隔离 |
| **子网** | VPC 内 IP 段 | 云资源必须部署在子网内；同一 VPC 内子网网段不可重复 |
| **安全组** | **ECS（实例）级** | 逻辑分组，为相同安全需求的 ECS 提供访问策略 |
| **网络 ACL** | **子网级** | 子网级别的可选安全层，控制出入子网的数据流 |

### 操作要点

1. **创建 VPC**：指定**私网网段**（支持 `10.0.0.0/8~24`、`172.16.0.0/12~24`、`192.168.0.0/16~24`）；系统自动创建**默认路由表**（保证同一 VPC 内子网互通）。
2. **创建子网**：子网网段必须在私网网段内；默认启用 **DHCP**，ECS 启动后自动获取 IP。
3. **配置安全组**：
   - 系统默认创建 **Sys-default** 安全组：**出方向全部放行、入方向受限**，组内 ECS 无需加规则即可互访；
   - 添加规则：**入方向**（从外部访问组内 ECS，如放行 22/3389/80）、**出方向**（组内 ECS 访问组外）。
4. **配置网络 ACL**：
   - 关联到子网，配置**出方向/入方向规则**，作为额外防护层；
   - 对比记忆：**安全组护 ECS，网络 ACL 护子网**，两者结合可实现更精细的访问控制。
5. **验证连通性**：
   - 同一 VPC 内不同子网的 ECS 默认互通；
   - 不同 VPC 的 ECS 默认不通，需通过**对等连接**（同区域内）打通；
   - 用 ping / 远程登录验证规则是否生效。

### 结论

- VPC 是云上网络的"地基"，安全组是"门禁"，网络 ACL 是"楼层的第二道门"。
- 排障思路：**先看安全组、再看网络 ACL、最后看路由与 EIP/带宽**。

---

## 9.4 数据库基础服务实践（RDS for MySQL）

### 实验目的

- 完成一台 **RDS for MySQL** 实例的创建；
- 熟悉通过**内网/外网**连接数据库并做基础操作；
- 理解云数据库相对自建数据库的优势（即开即用、弹性伸缩、易运维）。

### 涉及服务

| 服务 | 说明 |
| --- | --- |
| **RDS for MySQL** | 云数据库，最小管理单元是**实例**；实例类型分单机、主备等 |
| **VPC/子网/安全组** | 数据库实例运行其中，需放行数据库端口 |
| **ECS** | 作为"跳板机"从内网连接数据库 |

### 操作要点

1. **创建实例**：
   - 选择引擎版本、计费模式、区域/可用区、实例类型（单机 / 主备）；
   - 设置 VPC 与子网、安全组；设置管理员密码；选择存储空间。
2. **网络与安全**：
   - 若从 ECS 内网连接：ECS 与 RDS 需在同一 VPC（或已打通）；
   - 在 RDS 绑定的安全组中**放行 MySQL 端口（3306）**，来源指定 ECS 的私网 IP；
   - 如需公网访问，绑定 **EIP** 并放行相应规则（生产环境不推荐）。
3. **连接数据库**：
   - 登录一台 Linux ECS，安装 MySQL 客户端；
   - 使用 `mysql -h <实例IP/域名> -P 3306 -u <用户名> -p` 连接；
   - 也可使用 Database 管理工具连接。
4. **基础操作**：建库建表、增删改查，验证读写正常。
5. **了解进阶特性**（选做）：**跨 AZ 高可用**、**读写分离**（创建只读实例，最多 5 个）、**任意时间点恢复 PITR**（基于 Binlog）、备份与日志管理。

### 结论

- RDS 把"装数据库、打补丁、做备份、搭主备"等运维工作交给云平台，用户**即开即用、按需伸缩**。
- 水平伸缩 = 增删只读实例（最多 5 个）；垂直伸缩 = 规格变更 + 存储扩容（最大 4 TB）。
- 连接前务必确认：**VPC 是否互通 + 安全组是否放行端口**。

---

## 9.5 基础弹性架构部署实践（AS）

### 实验目的

- 搭建一套"**ELB + AS + ECS**"的**基础弹性架构**；
- 配置**伸缩策略**并观察实例自动扩缩；
- 做**稳定性测试**，验证架构在压力下的自愈与扩容能力。

### 涉及服务

| 服务 | 角色 |
| --- | --- |
| **AS** | 弹性伸缩，自动增减 ECS 实例 |
| **ELB** | 弹性负载均衡，把流量分发到伸缩组内所有实例 |
| **ECS** | 承载业务的后端服务器 |
| **VPC/子网/安全组** | 网络底座 |
| **CES（云监控）** | 提供 CPU 等监控指标，触发告警策略 |

### 操作要点

1. **准备网络**：创建 VPC、子网、安全组（放行业务端口与健康检查端口）。
2. **创建伸缩配置**：
   - 方式一：以**已有 ECS 快速创建**（vCPU、内存、镜像、磁盘等默认与所选 ECS 一致）；
   - 方式二：**使用新模板**创建全新配置。
3. **创建伸缩组**：
   - 配置**多可用区扩展策略**（选 2 个及以上 AZ 时）；
   - 设置**最大 / 期望 / 最小实例数**（期望值介于最小与最大之间）；
   - 设置**实例移除策略**、**健康检查**方式；
   - **绑定 ELB**：把伸缩组关联到 ELB 的后端服务器组。
4. **创建伸缩策略**：
   - **告警策略**：如 CPU 使用率 > 70% 扩容、< 30% 缩容；
   - **定时 / 周期策略**：如每天固定时段扩容；
   - 设置**冷却时间**（秒），避免告警策略频繁触发。
5. **稳定性测试**：
   - 在 ECS 上（或通过压测工具）制造 CPU 压力，观察是否触发扩容、新实例是否**自动加入 ELB**；
   - 停止压力，观察冷却期后是否自动缩容；
   - 故意停止/破坏一台实例，验证**健康检查**是否会替换不健康实例。
6. **释放资源**：删除伸缩组/策略、ELB、ECS 等，避免持续计费。

### 结论

- **ELB + AS** 是云上弹性架构的经典组合：AS 负责"有多少台"，ELB 负责"流量怎么分"；
- 冷却时间保证扩缩容**不被频繁触发**，让系统稳定；
- 弹性架构同时收获**弹性（省成本）**与**高可用（容错、消除单点）**。

---

## 核心概念速查表（实验版）

| 术语 | 实验中的关键点 |
| --- | --- |
| ECS 计费模式 | 按需 / 包周期 / 竞价 |
| 镜像四类 | 公共 / 私有 / 共享 / 市场 |
| EVS 容量 | 10 GB ~ 32 TB，扩容步长 1 GB |
| OBS 组成 | 桶 Bucket + 对象 Object |
| SFS 协议 | NFS（Linux）/ CIFS（Windows），Linux 仅 NFSv3 |
| VPC 私网网段 | 10.0.0.0/8~24、172.16.0.0/12~24、192.168.0.0/16~24 |
| 安全组 vs 网络 ACL | 安全组护 ECS（实例级），网络 ACL 护子网（子网级） |
| RDS 最小管理单元 | 实例（单机 / 主备） |
| RDS 只读实例 | 最多 5 个；可开通读写分离 |
| AS 三步 | 伸缩配置 → 伸缩组 → 伸缩策略 |
| 冷却时间 | 只约束告警策略触发的伸缩活动 |

---

## 考点 / 易错点

- **顺序不能乱**：网络（VPC/子网/安全组）→ 计算/存储/数据库 → 弹性（AS/ELB）。
- **EVS 不能单独使用**，必须挂载到 ECS/BMS 并初始化。
- **SFS 挂载到 Linux 只支持 NFSv3**；SFS Turbo 无需域名解析步骤。
- **安全组是实例级、网络 ACL 是子网级**，两者默认规则不同（默认安全组：出方向全放行、入方向受限）。
- **RDS 连不上先查两件事**：VPC 是否互通、安全组是否放行 3306。
- **AS 的期望实例数**必须在最小与最大之间；**多可用区扩展策略**只在选 2 个及以上 AZ 时配置。
- **实验结束务必释放资源**（ECS、EVS、OBS 桶、SFS、RDS、ELB、AS、EIP），否则会持续扣费。
- 公网访问数据库（绑 EIP）**仅用于实验**，生产环境应走内网。

---

## 自测题

**1. 云硬盘（EVS）使用前必须完成的操作是（　）**
A. 绑定弹性公网 IP
B. 挂载到 ECS/BMS 并初始化
C. 配置负载均衡
D. 创建伸缩组

> **答案：B。** EVS 不能单独使用，需挂载并初始化、创建文件系统。

**2. 将 SFS 文件系统挂载到 Linux 云服务器时，使用的命令是（　）**
A. `net use` B. `mount -t nfs -o vers=3,timeo=600 挂载地址 本地路径` C. `mkdir` D. `nslookup`

> **答案：B。**

**3. 关于安全组与网络 ACL，说法正确的是（　）**
A. 安全组保护子网，网络 ACL 保护 ECS
B. 安全组保护 ECS，网络 ACL 保护子网
C. 两者都是实例级
D. 两者都必须在创建 VPC 时创建

> **答案：B。**

**4. RDS for MySQL 实例最大可创建多少个只读实例？（　）**
A. 1 个 B. 3 个 C. 5 个 D. 不限

> **答案：C。**

**5.（多选）搭建"ELB + AS"基础弹性架构时，伸缩组需要配置哪些参数？（　）**
A. 最大/期望/最小实例数
B. 实例移除策略、健康检查
C. 是否绑定 ELB
D. 数据库引擎版本

> **答案：ABC。** 数据库引擎版本属于 RDS 的参数。

**6. 实验做完后最容易被忽略、却非常重要的一步是（　）**
A. 再买一台 ECS
B. 释放所有实验资源
C. 更换账号
D. 关闭浏览器

> **答案：B。** 不释放资源会持续计费或耗尽额度。
